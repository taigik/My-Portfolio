パスワード再設定について

以下のURLからパスワードを再設定してください。

<%= edit_password_reset_url(@user.reset_token,
                                                      email: @user.email) %>

このリンクの有効期限は2時間です。

※パスワードの再設定を行わない場合は無視してください。
※本メールに心当たりがない場合は無視してください。
